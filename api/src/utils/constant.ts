export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}

export enum MatchStatus {
  MATCHED = 'matched',
  SCHEDULED = 'scheduled',
  NO_COMMON_SLOT = 'no_common_slot',
}

export enum AvailabilityBlock {
  MORNING = 'morning',
  AFTERNOON = 'afternoon',
  EVENING = 'evening',
}